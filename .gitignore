# Data & generated artifacts
finance.db
finance.db-wal
finance.db-shm
output/
archive/
inbox/*
!inbox/.gitkeep

# Raw statements (personal financial data — never commit)
Docs/

# Raw handover note (personal detail) — kept local; the sanitized PRD.md is committed
finance-tracker-handover-prompt.md

# Secrets
.env
*.token

# Personal config — real values stay local; only the sanitized *.example.yaml is committed
config/accounts.yaml
config/categories.yaml
config/iban_map.yaml
config/notion.yaml
config/learned.yaml

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
build/
dist/
